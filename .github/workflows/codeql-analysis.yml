name: CodeQL Analysis
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '00 18 * * 1'
  workflow_dispatch:

permissions: read-all

jobs:
  analyze-actions:
    name: CodeQL Analysis
    permissions:
      security-events: write
    uses: fish-shop/workflows/.github/workflows/codeql-analysis.yml@c81fa354baef57927430e24b14f2e467b55bd231 # v1.14.23
